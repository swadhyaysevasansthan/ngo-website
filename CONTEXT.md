# CONTEXT.md

# Swadhyay Seva Foundation --- QR Event Pass & Check-In System

## 1. Project Overview

We are adding a **QR-based event pass management and physical event
check-in system** to the existing Swadhyay Seva Foundation website and
creating a companion Android scanner application.

The system is intended for an upcoming physical event with **100+
attendees excluding chief guests and other special invitees**.

There will be **no public registration by attendees** for this pass
system. Swadhyay Seva Foundation will prepare/import the guest list in
the admin panel, generate unique passes with QR codes, distribute/print
the passes, and scan them at the event entrance.

The central business requirement is:

> A QR pass is valid only if it was generated by the Swadhyay admin
> system for the selected event, has not been cancelled, and has not
> already been checked in.

A successful scan changes:

`ISSUED → CHECKED_IN`

The same QR must never successfully check in twice.

The **backend/database is the final authority** for pass validity and
check-in status.

------------------------------------------------------------------------

# 2. Existing Website Repository

GitHub repository:

`https://github.com/swadhyaysevasansthan/ngo-website.git`

Before modifying anything, inspect the actual repository.

Do NOT assume file names, routes, database models, authentication, or
folder structure.

The existing website stack is:

-   React.js
-   Tailwind CSS
-   Node.js
-   Express.js
-   PostgreSQL
-   Existing admin panel/authentication
-   Existing API architecture

Production infrastructure:

-   Backend: Render
-   PostgreSQL: Aiven

The new system must integrate into the existing
website/backend/database.

Do not create an unnecessary parallel backend or database.

------------------------------------------------------------------------

# 3. Mobile Application Stack

The companion scanner application will use:

-   React Native
-   Expo
-   TypeScript
-   Expo Router
-   Expo Go on Android for development/testing
-   EAS Build for standalone APKs and production Android builds

The broader full-stack architecture is:

-   React / Next.js
-   Node.js / Express
-   PostgreSQL
-   Prisma where appropriate

The mobile application will communicate with the Node.js REST API.

IMPORTANT:

The mobile APK must NEVER connect directly to PostgreSQL/Aiven.

Correct architecture:

`Expo APK → HTTPS REST API → Node/Express on Render → PostgreSQL on Aiven`

------------------------------------------------------------------------

# 4. Overall System Architecture

``` text
                         SWADHYAY ADMIN PANEL
                                  |
                                  v
                         Node / Express API
                                  |
                                  v
                         PostgreSQL (Aiven)
                                  ^
                                  |
                         Node / Express API
                                  ^
                                  |
                              HTTPS
                                  ^
                                  |
                    React Native Expo APK
                         Event Scanner
                                  |
                                  v
                              QR Camera
```

The admin website and scanner APK use the same backend and database.

Multiple scanner phones must be able to operate simultaneously.

Example:

-   Main Gate --- Phone 1
-   Main Gate --- Phone 2
-   VIP Gate --- Phone 1

If Phone 1 checks in Pass #047, Phone 2 must immediately receive
`ALREADY_CHECKED_IN` if it scans the same pass afterward.

------------------------------------------------------------------------

# 5. Development Philosophy

Work in **small, independently verifiable phases**.

Do NOT attempt to implement the entire website + backend + APK in one
huge task.

After every significant phase:

1.  Verify/test the changes.
2.  Update this `CONTEXT.md` immediately.
3.  Record:
    -   What was completed
    -   Files modified
    -   Important decisions
    -   Tests performed
    -   Current status
    -   Next step
    -   Any unresolved issue
4.  Commit the changes to Git when appropriate.

Do NOT wait until the end of the entire project to update `CONTEXT.md`.

The context file is a project handoff/recovery document.

------------------------------------------------------------------------

# 6. Git Recovery Strategy

Git is an additional source of truth.

If a Claude session ends unexpectedly, the next session must inspect:

``` bash
git status
git diff
git log --oneline -10
```

Then inspect:

1.  `CLAUDE.md`
2.  `CONTEXT.md`
3.  `git status`
4.  `git diff`
5.  `git log --oneline -10`

Compare the actual codebase with `CONTEXT.md`.

IMPORTANT:

`CONTEXT.md` may be outdated if the previous session ended before
updating it.

Never assume the context file is completely accurate.

If actual code changes exist after the last documented checkpoint:

-   Identify them.
-   Determine what they do.
-   Update `CONTEXT.md`.
-   Then continue.

Never overwrite or discard working changes merely because they are not
documented in `CONTEXT.md`.

------------------------------------------------------------------------

# 7. Recommended Phase Plan

## PHASE 0 --- Repository Reconnaissance

Goal:

Understand the existing application before modifying it.

Inspect:

-   Frontend architecture
-   Backend architecture
-   Admin panel
-   Authentication
-   Database connection
-   Existing database schema
-   Prisma setup, if present
-   Existing API conventions
-   Existing controllers/services
-   Existing routes
-   Existing environment variables
-   Deployment configuration
-   Existing reusable UI components
-   Existing Tailwind design conventions

Do not implement the feature yet.

Deliver:

-   Architecture summary
-   Relevant file list
-   Integration points
-   Proposed database design
-   Proposed API design
-   Proposed admin UI structure
-   Risks/compatibility concerns

Then update `CONTEXT.md`.

------------------------------------------------------------------------

# 8. PHASE 1 --- Database Design and Migration

Inspect the existing database architecture first.

If Prisma is already used, extend the existing Prisma schema.

If Prisma is not used in the existing website backend, follow the
repository's existing database approach rather than introducing a second
database abstraction unnecessarily.

Create the data model required for:

## Event

Suggested fields:

``` text
id
name
slug
description
event_date
venue
status
created_at
updated_at
```

## EventPass

Suggested fields:

``` text
id
event_id
pass_number
guest_name
mobile
email
category
qr_token
status
issued_at
checked_in_at
checked_in_by
gate
created_at
updated_at
```

Suggested status values:

``` text
ISSUED
CHECKED_IN
CANCELLED
```

## ScannerDevice / ScannerUser

Suggested fields:

``` text
id
name
scanner_code
user_id
gate
is_active
created_at
updated_at
```

Reuse the existing authentication/user model where possible.

## CheckInLog

Suggested fields:

``` text
id
event_id
pass_id
scanner_id
gate
action
result
scanned_at
ip_address
user_agent
```

Possible results:

``` text
SUCCESS
ALREADY_CHECKED_IN
INVALID
CANCELLED
WRONG_EVENT
```

Adapt the exact schema to the existing project.

### Critical requirement

Implement database-level/application-level protection against concurrent
duplicate check-ins.

Two simultaneous requests for the same pass must result in:

-   One `SUCCESS`
-   One `ALREADY_CHECKED_IN`

Never two successful check-ins.

After implementation:

-   Run migration
-   Verify schema
-   Test constraints/indexes
-   Update `CONTEXT.md`

------------------------------------------------------------------------

# 9. PHASE 2 --- Secure QR Token Generation

Every pass needs two identifiers:

### Human-readable Pass Number

Example:

``` text
SNEPC-2026-0001
SNEPC-2026-0002
SNEPC-2026-0003
```

### Secure QR Token

A cryptographically secure random token.

Do NOT use sequential IDs as security tokens.

Do NOT put the guest's name into the QR as the authorization mechanism.

The QR should encode a secure token or an appropriate secure
verification URL.

Example concept:

``` text
https://swadhyayseva.org/event-pass/verify/<secure-token>
```

The exact implementation should be chosen after inspecting the existing
website architecture.

The backend must be the authority for token validity.

After implementation:

-   Test token uniqueness
-   Test invalid tokens
-   Test cancelled tokens
-   Test expired/reissued tokens if reissue functionality is implemented
-   Update `CONTEXT.md`

------------------------------------------------------------------------

# 10. PHASE 3 --- Backend APIs

Implement the backend APIs using the existing Express architecture.

Suggested functionality:

``` text
Create Event
List Events
Get Event
Update Event
Create Pass
Bulk Import Passes
List Passes
Get Pass
Cancel Pass
Reissue QR
Check-In Pass
Get Check-In Status
Scanner Login
Get Scanner Profile
Attendance Statistics
Check-In Logs
```

Suggested endpoints, subject to existing API conventions:

``` text
POST /api/event-passes/events
GET  /api/event-passes/events
GET  /api/event-passes/events/:id
PUT  /api/event-passes/events/:id

POST /api/event-passes
POST /api/event-passes/import
GET  /api/event-passes
GET  /api/event-passes/:id
POST /api/event-passes/:id/cancel
POST /api/event-passes/:id/reissue

POST /api/event-passes/check-in
GET  /api/event-passes/check-in-status/:token

POST /api/scanner/login
GET  /api/scanner/me

GET /api/event-passes/attendance/:eventId
GET /api/event-passes/check-in-logs/:eventId
```

Use the existing route naming conventions if they differ.

------------------------------------------------------------------------

# 11. PHASE 4 --- Check-In API

This is the most critical backend component.

Suggested endpoint:

``` text
POST /api/event-passes/check-in
```

Request concept:

``` json
{
  "token": "SECURE_QR_TOKEN",
  "eventId": "EVENT_ID",
  "scannerId": "SCANNER_ID",
  "gate": "MAIN_GATE"
}
```

Backend verification sequence:

1.  Authenticate scanner.
2.  Validate request.
3.  Find token.
4.  Confirm event exists and is active.
5.  Confirm pass belongs to event.
6.  Confirm pass is not cancelled.
7.  Atomically verify pass is still `ISSUED`.
8.  Change status to `CHECKED_IN`.
9.  Record timestamp.
10. Record scanner/user.
11. Record gate.
12. Create immutable CheckInLog.
13. Return result.

Success response should include:

``` text
success
result = SUCCESS
message
passNumber
guestName
category
checkedInAt
gate
```

Duplicate response:

``` text
success = false
result = ALREADY_CHECKED_IN
message
checkedInAt
gate
```

Invalid response:

``` text
success = false
result = INVALID
message
```

Cancelled response:

``` text
success = false
result = CANCELLED
message
```

Wrong event response:

``` text
success = false
result = WRONG_EVENT
message
```

The backend must never trust guest information supplied by the mobile
client.

------------------------------------------------------------------------

# 12. PHASE 5 --- Scanner Authentication

The scanner APK must be authenticated.

Do not expose the check-in endpoint as an unrestricted public endpoint.

Reuse the existing authentication system where appropriate.

Possible flow:

``` text
Scanner Login
    ↓
Backend authentication
    ↓
JWT/access token
    ↓
Secure storage on APK
    ↓
Authenticated scan requests
```

Scanner devices should be identifiable.

Example:

``` text
MAIN-GATE-01
MAIN-GATE-02
VIP-GATE-01
```

Every scan should be associated with the scanner.

Admin must be able to disable a scanner.

------------------------------------------------------------------------

# 13. PHASE 6 --- Admin Panel

Add an **Event Passes** module to the existing admin panel.

Follow the existing admin UI and Tailwind conventions.

Do not redesign unrelated parts of the admin panel.

Suggested structure:

``` text
Admin
 └── Event Passes
      ├── Events
      ├── Passes
      ├── Create Pass
      ├── Import Guests
      ├── Generate Passes
      ├── Attendance
      └── Scanner Devices
```

Admin functionality:

### Events

-   Create
-   Edit
-   Activate/deactivate
-   View statistics

### Passes

-   Create
-   Search
-   Filter
-   View
-   Cancel
-   Reissue
-   Print
-   Download

### Bulk Import

Input:

``` text
Name
Mobile
Email
Category
```

Required behavior:

-   File validation
-   Preview
-   Duplicate detection
-   Invalid-row detection
-   Pass generation
-   Import summary

Example:

``` text
150 uploaded
145 created
3 duplicates
2 invalid
```

Never silently create duplicate passes.

------------------------------------------------------------------------

# 14. PHASE 7 --- QR/Printable Pass

Generate a printable pass for each guest.

Suggested contents:

``` text
SWADHYAY SEVA FOUNDATION

EVENT NAME

GUEST PASS

Guest Name
Pass Number
Category

[ QR CODE ]

Please present this pass at the entry gate.
```

The existing Swadhyay branding should be respected.

Provide:

-   Individual pass preview
-   Print
-   Download
-   Bulk generation/printing if practical

If QR reissue is supported:

-   Generate a new secure token.
-   Invalidate the old token.
-   Ensure the old QR cannot be used.

------------------------------------------------------------------------

# 15. PHASE 8 --- Attendance Dashboard

Admin dashboard should show:

``` text
Total Passes
Checked In
Pending
Cancelled
Check-In %
```

Also show recent activity:

``` text
Time
Guest
Pass Number
Category
Gate
Scanner
Result
```

Provide filters:

``` text
Name
Pass Number
Category
Status
Gate
Date/Time
```

Provide CSV export if practical.

------------------------------------------------------------------------

# 16. PHASE 9 --- Manual Check-In

Provide a fallback for damaged/unreadable QR codes.

Search by:

-   Name
-   Pass number
-   Mobile

Admin/authorized staff can manually check in.

Manual check-in must create an audit record:

``` text
action = MANUAL_CHECKIN
```

Do not bypass the normal status/audit rules.

------------------------------------------------------------------------

# 17. PHASE 10 --- Backend Testing

Before building the APK, test the backend thoroughly.

Required tests:

### Valid scan

``` text
ISSUED → CHECKED_IN
```

### Duplicate scan

``` text
CHECKED_IN → ALREADY_CHECKED_IN
```

### Invalid QR

``` text
INVALID
```

### Cancelled pass

``` text
CANCELLED
```

### Wrong event

``` text
WRONG_EVENT
```

### Unauthorized scanner

``` text
401/403
```

### Concurrent scanning

Two simultaneous requests for the same pass:

``` text
Request A → SUCCESS
Request B → ALREADY_CHECKED_IN
```

Verify the database state afterward.

Do not move to APK development until the API behavior is reliable.

Update `CONTEXT.md` after testing.

------------------------------------------------------------------------

# 18. PHASE 11 --- Mobile APK Project

Create a separate React Native Expo TypeScript application for the
scanner.

Technology:

-   React Native
-   Expo
-   TypeScript
-   Expo Router
-   Expo Go for development
-   EAS Build for APK

The APK is an internal event scanner.

Suggested routes:

``` text
app/
  _layout.tsx
  index.tsx
  login.tsx
  scanner.tsx
  result.tsx
  profile.tsx
```

Adapt the structure to the latest Expo Router conventions.

------------------------------------------------------------------------

# 19. APK Login

Login screen:

``` text
SWADHYAY EVENT SCANNER

Scanner ID / Username
Password

[ LOGIN ]
```

After authentication:

-   Store access token securely.
-   Restore session on app restart.
-   Handle token expiry.
-   Logout.
-   Redirect unauthorized users to login.

Do not put admin credentials or backend secrets inside the APK.

------------------------------------------------------------------------

# 20. APK Scanner

Main scanner screen:

``` text
SWADHYAY

EVENT NAME

Scanner:
Main Gate - Phone 1

[ CAMERA SCANNER ]

Point camera at QR pass
```

Use a current Expo-compatible QR/barcode scanning library.

Before selecting a library, verify that it works with the current Expo
SDK.

Scanner behavior:

1.  Open camera.
2.  Detect QR.
3.  Extract token.
4.  Prevent duplicate rapid scans.
5.  Send token to backend.
6.  Display result.
7.  Return quickly to scanner.
8.  Allow next scan.

------------------------------------------------------------------------

# 21. APK Result States

## Valid

``` text
✓ ENTRY APPROVED

Rahul Sharma
SNEPC-2026-0047
Guest

Checked in: 10:42 AM

[ SCAN NEXT ]
```

## Already Checked In

``` text
⚠ ALREADY CHECKED IN

Rahul Sharma
SNEPC-2026-0047

Checked in: 10:42 AM
Gate: Main Gate

[ SCAN NEXT ]
```

## Invalid

``` text
✕ INVALID PASS

This QR code is not valid.

[ SCAN NEXT ]
```

## Cancelled

``` text
✕ PASS CANCELLED
```

## Connection Error

``` text
⚠ CONNECTION ERROR

Unable to contact the event server.

Check the internet connection.

[ RETRY ]
```

The APK must never display "approved" until the backend has actually
confirmed the check-in.

------------------------------------------------------------------------

# 22. APK API Client

Use a clean API abstraction.

Possible structure:

``` text
src/
  api/
    client.ts
    auth.ts
    checkin.ts
```

Use environment configuration.

Example:

``` text
EXPO_PUBLIC_API_URL
```

Do not hard-code localhost for production.

Remember that `localhost` on an Android phone refers to the phone
itself, not the development computer.

Support:

-   Development API
-   Production Render API

through appropriate environment configuration.

------------------------------------------------------------------------

# 23. Secure Storage

Use an Expo-compatible secure storage solution for authentication
tokens.

Do not store authentication credentials insecurely if avoidable.

Handle:

-   Login
-   Session restoration
-   Logout
-   Token expiry
-   Unauthorized API response

------------------------------------------------------------------------

# 24. Network Behavior

Version 1 should require internet connectivity.

Do NOT implement offline check-in initially.

Reason:

If two phones are offline, they cannot immediately know that the same
pass was already checked in on another phone.

The backend should remain the single source of truth.

If offline support is needed later, implement it as a separate phase
with explicit conflict resolution.

------------------------------------------------------------------------

# 25. APK Performance

The event may have 100+ attendees.

Optimize for rapid sequential scanning.

Ideal flow:

``` text
Scan
 ↓
API
 ↓
Result
 ↓
Short confirmation
 ↓
Scan next
```

Do not require multiple unnecessary navigation steps.

Prevent repeated API requests while a scan is processing.

------------------------------------------------------------------------

# 26. Security Requirements

Implement:

-   HTTPS in production
-   Authenticated scanner APIs
-   Authorization
-   Cryptographically secure QR tokens
-   Server-side verification
-   Atomic/transactional check-in
-   Rate limiting where appropriate
-   Input validation
-   Audit logging
-   Scanner activation/deactivation
-   No database credentials in APK
-   No admin secrets in APK
-   No direct mobile-to-PostgreSQL connection

The client must never be trusted to decide whether a pass is valid.

------------------------------------------------------------------------

# 27. Environment Configuration

Document new variables in `.env.example`.

Potential examples:

``` text
QR_PASS_TOKEN_SECRET
EVENT_SCANNER_JWT_SECRET
```

Only add variables that are genuinely required by the implementation.

Never commit production secrets.

------------------------------------------------------------------------

# 28. Deployment

## Website/backend

Keep the existing production infrastructure:

``` text
Frontend → existing hosting
Backend → Render
Database → Aiven PostgreSQL
```

Do not create a second production backend.

## APK

Development:

``` text
Expo Go
```

Production:

``` text
EAS Build
    ↓
Standalone Android APK
```

The production APK should use the production Render API URL.

------------------------------------------------------------------------

# 29. Documentation Requirements

Document:

1.  Database changes
2.  Environment variables
3.  API endpoints
4.  Authentication
5.  Admin workflow
6.  Pass generation workflow
7.  Scanner workflow
8.  Local development
9.  Expo Go testing
10. EAS APK build
11. Production deployment
12. Troubleshooting

------------------------------------------------------------------------

# 30. CONTEXT.md Checkpoint Rule

After every significant phase, immediately update this file.

Use this format:

``` markdown
## Current Status

Phase: PHASE X — [NAME]
Status: COMPLETE / IN PROGRESS / BLOCKED

### Completed

- ...
- ...

### Files Modified

- ...
- ...

### Database Changes

- ...
- ...

### APIs Added/Changed

- ...
- ...

### Tests Performed

- ...
- ...

### Important Decisions

- ...
- ...

### Known Issues

- ...
- ...

### Next Step

- ...
```

Do not merely say "Phase completed."

Record enough information that another Claude session can continue
without guessing.

------------------------------------------------------------------------

# 31. Git Checkpoint Rule

After each major phase:

1.  Run tests/build/lint as appropriate.
2.  Review `git diff`.
3.  Review `git status`.
4.  Commit stable work with a descriptive commit message.

Example:

``` text
feat: add event pass database models
```

``` text
feat: add event pass check-in APIs
```

``` text
feat: add event pass admin module
```

``` text
feat: add Expo event scanner
```

Do not commit secrets.

------------------------------------------------------------------------

# 32. Recovery Procedure

If a previous Claude session may have ended unexpectedly:

First run:

``` bash
git status
git diff
git log --oneline -10
```

Then read:

``` text
CLAUDE.md
CONTEXT.md
```

Compare the documented state with the actual code.

Determine:

-   What was completed
-   What was partially completed
-   What files changed
-   Whether migrations were run
-   Whether tests passed
-   Whether the APK project exists
-   Whether the API contract changed

Update `CONTEXT.md` before continuing.

Do not restart completed work unnecessarily.

Do not discard uncommitted changes without understanding them.

------------------------------------------------------------------------

# 33. Working Style for Claude

When working on this project:

-   Work in small stages.
-   Inspect before modifying.
-   Do not guess existing architecture.
-   Reuse existing components and conventions.
-   Keep unrelated code untouched.
-   Verify each change.
-   Update `CONTEXT.md` immediately after meaningful checkpoints.
-   Keep Git history useful.
-   Explain blockers precisely.
-   Do not move to the next major phase if a critical previous phase is
    broken.
-   Do not generate the entire project in one response.
-   Prefer incremental implementation and testing.

------------------------------------------------------------------------

# 34. Initial Task

When beginning work on this project, do NOT immediately implement the QR
system.

First perform:

## PHASE 0 --- Repository Reconnaissance

Inspect:

``` text
CLAUDE.md
CONTEXT.md
package.json
frontend structure
backend structure
database structure
Prisma/schema files if present
authentication
admin panel
API routes
environment configuration
deployment configuration
```

Then provide:

1.  Current architecture
2.  Relevant files
3.  Existing authentication mechanism
4.  Existing database approach
5.  Best integration points
6.  Proposed event-pass schema
7.  Proposed API architecture
8.  Proposed admin module structure
9.  Proposed Expo APK architecture
10. Potential risks

After completing this reconnaissance:

**Update `CONTEXT.md` immediately.**

Do not begin Phase 1 until Phase 0 is documented.

------------------------------------------------------------------------

# 35. Final Target

The completed system should provide this workflow:

## Before Event

``` text
Admin
 ↓
Create Event
 ↓
Import/Create Guests
 ↓
Generate Unique Passes
 ↓
Generate QR Codes
 ↓
Print/Distribute Passes
```

## At Event

``` text
Guest presents physical pass
 ↓
Volunteer opens Swadhyay Scanner APK
 ↓
Scans QR
 ↓
APK sends secure token to Render API
 ↓
Render API verifies token
 ↓
Aiven PostgreSQL transaction
 ↓
ISSUED → CHECKED_IN
 ↓
CheckInLog created
 ↓
APK shows:
🟢 ENTRY APPROVED
```

## Duplicate

``` text
Same QR scanned again
 ↓
Backend sees CHECKED_IN
 ↓
APK shows:
🔴 ALREADY CHECKED IN
```

## Admin

``` text
Admin Dashboard

Total Passes: 150
Checked In: 87
Pending: 63

Recent Check-ins
Gate
Scanner
Time
Guest
Pass
Result
```

The system must be reliable enough for real-world event entry with
multiple simultaneous scanner devices.

------------------------------------------------------------------------

# 36. Current Project Status

Phase: PHASE 0 — Repository Reconnaissance

Status: **COMPLETE**

---

## Phase 0 Findings

### 1. Repository Layout

```
ngo-website/
├── CLAUDE.md
├── CONTEXT.md
├── backend/                 ← Node.js / Express backend (ES modules)
│   ├── database/            ← Raw SQL migration files + run_migration.js
│   ├── src/
│   │   ├── config/          ← database.js, email.js, cloudinary.js, razorpay.js, sneacEmail.js
│   │   ├── controllers/     ← One file per feature domain
│   │   ├── middleware/      ← auth.js, judgeAuth.js, rateLimiter.js, upload.js, validation.js
│   │   ├── models/          ← reviewModel.js (only one model file; most logic is inline SQL)
│   │   ├── routes/          ← One file per feature domain
│   │   ├── services/        ← conflictDetectionService, qualificationService, recaptchaService, reportService
│   │   ├── utils/           ← emailTemplates.js, idGenerator.js, judgeCredentials.js, tokenService.js, ...
│   │   └── server.js        ← Express entry point
│   └── package.json
└── ngo-web/                 ← React 18 frontend (CRA / react-scripts)
    ├── src/
    │   ├── components/      ← Shared + feature-specific components
    │   │   ├── admin/       ← FarmersAdminTab, FarmerForm, FarmerCategoriesPanel
    │   │   ├── evaluation/  ← EvaluationAdminTab, JudgeManagement, EvaluationResultsGrid, ...
    │   │   └── sneac/       ← SneacAdminTab, useSneacAdmin hook
    │   ├── layouts/         ← AdminLayout (thin Outlet wrapper), PublicLayout
    │   ├── pages/           ← One file per page; AdminDashboard is the monolithic admin SPA
    │   ├── routes/          ← AppRoutes, ProtectedRoute, PublicRoute, JudgeProtectedRoute, JudgePublicRoute
    │   └── utils/
    │       └── api.js       ← Axios client + all API namespaces (adminAPI, judgeAPI, evaluationAdminAPI, …)
    └── package.json
```

---

### 2. Frontend Architecture

- **Framework:** React 18 with CRA (`react-scripts 5`).
- **Router:** React Router v6 (`<Routes>` / `<Route>`).
- **Styling:** Tailwind CSS v3. Custom tokens: `primary` (amber-500), `forest` (#0f172a), `leaf`, `earth`, `sky`, `sunset`, `saffron`. Animations: `fade-in`, `slide-up`, `slide-down`, `scale-in`.
- **HTTP:** Single shared `apiClient` (Axios instance, base `REACT_APP_API_URL/api`). Separate `judgeApiClient` for judge routes (separate `judgeToken` in `localStorage`).
- **State:** Local `useState`/`useEffect`; `@tanstack/react-query` is installed but not widely used yet.
- **UI Libraries:** `lucide-react`, `react-icons`, `@heroicons/react`, `framer-motion`, `react-toastify`, `swiper`.
- **Key pages:** `AdminDashboard.jsx` (~880 lines) — monolithic, tab-based admin SPA. Tabs: `dashboard`, `participants`, `reviews`, `sneac`, `farmers`, `evaluation`, `email`.
- **Route guards:**
  - `ProtectedRoute` — checks `localStorage.getItem('token')` → `/admin/login`.
  - `JudgeProtectedRoute` — checks `localStorage.getItem('judgeToken')` → `/judge/login`.
- **No server-side rendering.** All rendering is client-side.

---

### 3. Backend Architecture

- **Runtime:** Node.js (ES modules — `"type": "module"` in package.json).
- **Framework:** Express 5.
- **Entry:** `backend/src/server.js` — registers all routes, CORS, helmet, JSON body parsing.
- **CORS origins:** `localhost:3000`, Vercel preview URL, `www.swadhyayseva.org`, plus `process.env.FRONTEND_URL`.
- **Deployment:** Render (backend), Aiven PostgreSQL (database).
- **No Prisma.** Raw `pg` Pool with parameterized queries throughout. No ORM.
- **Database config:** `src/config/database.js` — single `Pool` from `DATABASE_URL`, SSL enabled (`rejectUnauthorized: false`). Also exports a `query()` helper with timing logs.
- **Pattern:** Routes → Controllers → Direct `pool.query()` calls. No service layer except for evaluation.
- **PDF/Excel:** `pdfkit` + `exceljs` used for report/certificate generation.
- **File uploads:** `multer` + Cloudinary (`cloudinary.js`).
- **Email:** `resend` SDK (primary) via `config/email.js`; `nodemailer` also present; SNEAC has its own `sneacEmail.js`.
- **Installed but relevant for us:** `nanoid` (ID generation), `crypto` (built-in, used for tokens), `express-rate-limit`, `express-validator`, `bcryptjs`, `jsonwebtoken`.

---

### 4. Authentication & Authorization

Two completely separate auth systems exist side-by-side:

#### Admin Auth
| Item | Detail |
|---|---|
| Table | `admins` (id, username, password_hash, email, created_at) |
| Login endpoint | `POST /api/admin/login` |
| Token type | JWT signed with `JWT_SECRET`, 24h expiry |
| localStorage key | `token` |
| Middleware | `verifyAdmin` in `middleware/auth.js` — Bearer header, no DB re-check |
| Payload | `{ id, username }` |

#### Judge Auth
| Item | Detail |
|---|---|
| Table | `judges` (id, full_name, username, password_hash, is_active, last_login, last_activity, ...) |
| Login endpoint | `POST /api/judge/login` |
| Token type | JWT signed with `JWT_JUDGE_SECRET`, 12h expiry |
| localStorage key | `judgeToken` |
| Middleware | `verifyJudge` in `middleware/judgeAuth.js` — verifies role=`judge`, **re-checks `judges` table on every request** (handles deactivation mid-session) |
| Payload | `{ id, username, role: 'judge' }` |

**For the Scanner system** a third auth identity is needed: scanner devices. The `judgeAuth.js` pattern (separate secret, DB re-check for `is_active`) is the model to follow.

---

### 5. Database / PostgreSQL Setup

**No Prisma.** Raw SQL only.

#### Existing Tables (confirmed from SQL files)
| Table | Purpose |
|---|---|
| `participants` | Photography competition registrants |
| `payments` | Razorpay payment records |
| `submissions` | Photo submission records |
| `admins` | Admin user credentials |
| `email_logs` | Email send history |
| `judges` | Judge credentials (separate from admins) |
| `evaluation_competitions` | Competition definitions |
| `evaluation_entries` | Anonymized judge-visible entries |
| `evaluation_settings` | Per-competition toggle settings |
| `evaluation_scores` | Judge scores per entry/round |
| `evaluation_score_audit` | Immutable score change log |
| `evaluation_qualifications` | Round 1 derived results |
| `evaluation_winners` | Admin-assigned prize records |
| Community tables | (community_topics, images, albums, events, sections, stats) |
| Farmer tables | (farmers, farmer_categories) |
| SNEAC tables | (school_access_tokens, school_competition_registrations, competition_registration_teachers) |
| `visitors` | Visitor tracking |

#### Migration Strategy
- Migrations are plain `.sql` files in `backend/database/`.
- Applied manually by running `node backend/database/run_migration.js` (or `psql` directly).
- **No migration framework (no Flyway, Liquibase, node-pg-migrate, etc.).**
- The `run_migration.js` script is edited per migration — it is not a generic runner.

---

### 6. Existing API Conventions

All routes are under `/api/`. Pattern examples:

```
POST   /api/admin/login
GET    /api/admin/participants
GET    /api/admin/stats
POST   /api/admin/bulk-email

POST   /api/judge/login
GET    /api/judge/me
GET    /api/judge/dashboard
POST   /api/judge/entries/:entryId/score

GET    /api/admin/evaluation/judges
POST   /api/admin/evaluation/judges
PUT    /api/admin/evaluation/settings
GET    /api/admin/evaluation/results
POST   /api/admin/evaluation/qualify
GET    /api/admin/evaluation/export/:format

POST   /api/participants/register
POST   /api/submissions/submit
POST   /api/payments/create-order
```

**Conventions observed:**
- Admin-protected routes grouped under `/api/admin/` with `verifyAdmin` applied via `router.use()`.
- Domain-specific routes grouped by prefix (`/api/judge/`, `/api/farmers/`, `/api/communities/`).
- Response shape: `{ success: true|false, message, data?, count? }`.
- Validation via `express-validator` (`body()` validators) + `validateRequest` middleware.
- Rate limiting: `express-rate-limit` (currently only on registration endpoint).

---

### 7. Existing Utilities Available for Reuse

| Utility | Location | Relevance |
|---|---|---|
| `generateRawToken()` | `utils/tokenService.js` | `crypto.randomBytes(32).toString('hex')` — perfect for QR tokens |
| `hashToken()` | `utils/tokenService.js` | SHA-256 hash for DB storage |
| `generateParticipantId()` | `utils/idGenerator.js` | Pattern for pass number generation (`nanoid`) |
| `generateJudgeUsername/Password()` | `utils/judgeCredentials.js` | Pattern for scanner credential generation |
| `verifyAdmin` | `middleware/auth.js` | Admin route protection |
| `verifyJudge` | `middleware/judgeAuth.js` | **Exact template for `verifyScanner` middleware** |
| `validateRequest` | `middleware/validation.js` | Reuse for all new route validators |
| `rateLimiter` | `middleware/rateLimiter.js` | Extend/reuse for check-in endpoint |
| `pool` / `query()` | `config/database.js` | Use for all new DB queries |

---

### 8. Proposed Integration Points

#### Backend — New Files
```
backend/src/routes/
  eventPassRoutes.js        ← /api/event-passes/* (admin-managed)
  scannerRoutes.js          ← /api/scanner/* (scanner auth + check-in)

backend/src/controllers/
  eventPassController.js    ← CRUD for Events + Passes
  eventPassImportController.js ← Bulk import (Excel/CSV)
  checkInController.js      ← Atomic check-in logic
  scannerAuthController.js  ← Scanner login/me

backend/src/middleware/
  scannerAuth.js            ← verifyScanner (mirrors judgeAuth.js pattern)

backend/src/utils/
  passQRUtils.js            ← generatePassToken(), generatePassNumber()

backend/database/
  event_pass_migration.sql  ← New tables: events, event_passes, scanner_devices, checkin_logs
```

#### Backend — server.js Changes
Register two new route groups:
```js
import eventPassRoutes from './routes/eventPassRoutes.js';
import scannerRoutes from './routes/scannerRoutes.js';

app.use('/api/event-passes', eventPassRoutes);
app.use('/api/scanner', scannerRoutes);
```

#### Frontend — New Files
```
ngo-web/src/components/event-passes/
  EventPassAdminTab.jsx     ← Tab component (sub-tabs within AdminDashboard)
  EventsManager.jsx         ← Create/edit events
  PassList.jsx              ← Search/filter/view passes
  PassDetail.jsx            ← Individual pass + QR preview + print
  BulkImport.jsx            ← Excel upload + preview + import
  AttendanceDashboard.jsx   ← Live stats + check-in log
  ScannerDevices.jsx        ← Create/disable scanner devices
  ManualCheckIn.jsx         ← Fallback manual check-in search

ngo-web/src/utils/api.js    ← Add eventPassAPI and scannerAdminAPI namespaces
```

#### Frontend — AdminDashboard.jsx
Add a new tab entry:
```js
{ id: 'event-passes', label: '🎫 Event Passes' }
```
Render `<EventPassAdminTab />` when active. No other existing tab is modified.

#### Frontend — AppRoutes.jsx
No new protected routes are required for admin (stays within `/admin/dashboard` tab system). The scanner APK is a separate React Native project.

---

### 9. Proposed Database Schema

#### `events`
```sql
id            SERIAL PRIMARY KEY
name          VARCHAR(255) NOT NULL
slug          VARCHAR(100) UNIQUE NOT NULL
description   TEXT
event_date    DATE
venue         VARCHAR(255)
status        VARCHAR(20) DEFAULT 'draft' CHECK (status IN ('draft','active','completed','cancelled'))
created_at    TIMESTAMP WITH TIME ZONE DEFAULT NOW()
updated_at    TIMESTAMP WITH TIME ZONE DEFAULT NOW()
```

#### `event_passes`
```sql
id            SERIAL PRIMARY KEY
event_id      INTEGER NOT NULL REFERENCES events(id) ON DELETE RESTRICT
pass_number   VARCHAR(30) UNIQUE NOT NULL  -- e.g. SNEPC-2026-0001
guest_name    VARCHAR(255) NOT NULL
mobile        VARCHAR(20)
email         VARCHAR(255)
category      VARCHAR(100)                 -- VIP / Guest / Staff / etc.
qr_token      VARCHAR(128) UNIQUE NOT NULL -- crypto.randomBytes(32).toString('hex')
status        VARCHAR(20) DEFAULT 'ISSUED' CHECK (status IN ('ISSUED','CHECKED_IN','CANCELLED'))
issued_at     TIMESTAMP WITH TIME ZONE DEFAULT NOW()
checked_in_at TIMESTAMP WITH TIME ZONE
checked_in_by INTEGER REFERENCES scanner_devices(id)
gate          VARCHAR(100)
created_at    TIMESTAMP WITH TIME ZONE DEFAULT NOW()
updated_at    TIMESTAMP WITH TIME ZONE DEFAULT NOW()
```

#### `scanner_devices`
```sql
id            SERIAL PRIMARY KEY
name          VARCHAR(100) NOT NULL       -- e.g. "Main Gate – Phone 1"
device_code   VARCHAR(50) UNIQUE NOT NULL -- e.g. "MAIN-GATE-01"
password_hash VARCHAR(255) NOT NULL
event_id      INTEGER REFERENCES events(id)  -- optional: restrict to one event
gate          VARCHAR(100)
is_active     BOOLEAN DEFAULT true
last_seen_at  TIMESTAMP WITH TIME ZONE
created_at    TIMESTAMP WITH TIME ZONE DEFAULT NOW()
updated_at    TIMESTAMP WITH TIME ZONE DEFAULT NOW()
```

#### `checkin_logs`
```sql
id            SERIAL PRIMARY KEY
event_id      INTEGER NOT NULL REFERENCES events(id)
pass_id       INTEGER REFERENCES event_passes(id)
scanner_id    INTEGER REFERENCES scanner_devices(id)
gate          VARCHAR(100)
action        VARCHAR(30) NOT NULL  -- 'SCAN' | 'MANUAL_CHECKIN'
result        VARCHAR(30) NOT NULL  -- SUCCESS | ALREADY_CHECKED_IN | INVALID | CANCELLED | WRONG_EVENT
scanned_at    TIMESTAMP WITH TIME ZONE DEFAULT NOW()
ip_address    INET
user_agent    TEXT
raw_token     VARCHAR(128)         -- log what was actually scanned, for forensics
```

**Critical constraint (concurrent duplicate protection):**
```sql
-- Enforced in application via SELECT ... FOR UPDATE in a transaction;
-- PostgreSQL row-level locking ensures only one concurrent check-in wins.
-- No second unique constraint needed beyond qr_token UNIQUE on event_passes.
```

---

### 10. Proposed API Design

#### Admin-protected (Bearer admin JWT)
```
POST   /api/event-passes/events               Create event
GET    /api/event-passes/events               List events
GET    /api/event-passes/events/:id           Get event
PUT    /api/event-passes/events/:id           Update event

POST   /api/event-passes                      Create single pass
POST   /api/event-passes/import               Bulk import (Excel/CSV)
GET    /api/event-passes                      List passes (?eventId=&status=&search=)
GET    /api/event-passes/:id                  Get pass detail
POST   /api/event-passes/:id/cancel           Cancel pass
POST   /api/event-passes/:id/reissue          Regenerate QR token

GET    /api/event-passes/attendance/:eventId  Attendance stats
GET    /api/event-passes/checkin-logs/:eventId Check-in log

POST   /api/event-passes/manual-checkin       Manual fallback check-in

GET    /api/event-passes/scanners             List scanner devices
POST   /api/event-passes/scanners             Create scanner device
PATCH  /api/event-passes/scanners/:id/active  Enable/disable scanner
```

#### Scanner-authenticated (Bearer scanner JWT)
```
POST   /api/scanner/login    Scanner device login
GET    /api/scanner/me       Get scanner profile / assigned event
POST   /api/scanner/checkin  THE critical check-in endpoint
```

---

### 11. Proposed Admin UI Structure

New tab `🎫 Event Passes` inside `AdminDashboard.jsx` → renders `EventPassAdminTab.jsx`.

Sub-tabs inside `EventPassAdminTab`:
```
Events          → EventsManager.jsx
Passes          → PassList.jsx (search, filter, view, cancel, reissue, print)
Import Guests   → BulkImport.jsx (Excel/CSV upload, preview, import)
Attendance      → AttendanceDashboard.jsx (live stats + check-in log)
Scanner Devices → ScannerDevices.jsx (create, enable/disable)
Manual Check-In → ManualCheckIn.jsx (search by name/mobile/pass number)
```

Follows exact same sub-tab pattern as `EvaluationAdminTab.jsx`.

---

### 12. Proposed Expo APK Architecture

Separate project (not inside this repo):
```
scanner-app/   (new Expo + TypeScript project)
  app/
    _layout.tsx        ← Root layout, auth guard
    index.tsx          ← Redirect to /login or /scanner
    login.tsx          ← Scanner ID + password form
    scanner.tsx        ← Camera QR scanner (main screen)
    result.tsx         ← Check-in result display
    profile.tsx        ← Device info + logout
  src/
    api/
      client.ts        ← Axios instance (EXPO_PUBLIC_API_URL)
      auth.ts          ← login(), me()
      checkin.ts       ← checkin(token)
    hooks/
      useAuth.ts
      useCheckin.ts
    store/
      authStore.ts     ← SecureStore token persistence
```

Token stored with `expo-secure-store`. QR scanning via `expo-camera` + `expo-barcode-scanner` (or `react-native-vision-camera` depending on Expo SDK version).

---

### 13. New Environment Variables Required

**Backend (`backend/.env`):**
```
QR_PASS_JWT_SECRET=<strong-random-secret>   ← for scanner JWT (mirrors JWT_JUDGE_SECRET pattern)
```
No other new backend env vars are needed — QR tokens are stored as raw hex, not JWT.

**Frontend (`ngo-web/.env`):**
No new variables needed (uses same `REACT_APP_API_URL`).

**Expo APK (`.env` / `app.config.js`):**
```
EXPO_PUBLIC_API_URL=https://your-render-backend.onrender.com
```

---

### 14. Risks & Compatibility Notes

| Risk | Mitigation |
|---|---|
| No migration framework — SQL files applied manually | Continue same pattern; create `event_pass_migration.sql`, document exact run command |
| Atomic check-in under concurrent load | Use `BEGIN ... SELECT FOR UPDATE ... UPDATE ... COMMIT` transaction in PostgreSQL; never `UPDATE` without first locking the row |
| `run_migration.js` is not a generic runner — it contains hard-coded SQL | Write the new migration as a standalone `.sql` file; apply with `psql` directly or a one-off node script |
| No model layer — all logic in controllers | Follow existing pattern; keep inline SQL in controller functions |
| Admin dashboard is a monolith (~880-line single file) | Add tab entry + delegate to `EventPassAdminTab` component; don't restructure existing tabs |
| Frontend uses `localStorage` for auth — no HttpOnly cookies | Match existing pattern; scanner APK uses `expo-secure-store` (safe on mobile) |
| `nanoid` already installed — use for pass number suffixes | No new deps needed for ID generation |
| `pdfkit` already installed — usable for printable pass PDF generation | No new deps needed for PDF |
| QR code image generation in browser (admin UI) | Use `qrcode` npm package (small, no native deps) — add to frontend |
| `exceljs` already installed in backend — usable for bulk import parsing | No new backend deps needed |
| CORS: scanner APK hits same Render backend | APK is not a browser origin; CORS is irrelevant for native apps. No CORS change needed |
| Render cold-start latency | Out of scope for Phase 0; document in APK error-handling phase |

---

### 15. Files to Create in Phase 1

```
backend/database/event_pass_migration.sql
```

### Files to Modify in Phase 1

None — Phase 1 is additive SQL only.

---

## Phase 1 Checkpoint

Phase: PHASE 1 — Database Design and Migration

Status: **COMPLETE**

### Completed

- Created `backend/database/event_pass_migration.sql` with all four new tables.
- Created `backend/database/run_event_pass_migration.js` (transactional runner).
- Migration was **executed manually** by the user against the Aiven PostgreSQL database.
- The agent did NOT run the migration script.

### Files Modified

- `backend/database/event_pass_migration.sql` ← **new file**
- `backend/database/run_event_pass_migration.js` ← **new file**

### Database Changes

Four new tables added (additive only — no existing tables modified):

| Table | Purpose |
|---|---|
| `events` | One row per physical event; `status` ∈ draft/active/completed/cancelled |
| `scanner_devices` | One row per scanner phone/tablet; `device_code` + bcrypt `password_hash`; `is_active` flag |
| `event_passes` | One row per guest pass; `qr_token` (UNIQUE, 64-char hex); `status` ∈ ISSUED/CHECKED_IN/CANCELLED |
| `checkin_logs` | Append-only scan audit log; every attempt recorded regardless of result |

Indexes created:

- `idx_events_status`, `idx_events_slug`
- `idx_scanner_devices_event`, `idx_scanner_devices_active`
- `idx_event_passes_qr_token` (UNIQUE — hot path), `idx_event_passes_event`, `idx_event_passes_status`, `idx_event_passes_guest`, `idx_event_passes_mobile`, `idx_event_passes_number`
- `idx_checkin_logs_event`, `idx_checkin_logs_pass`, `idx_checkin_logs_scanner`, `idx_checkin_logs_scanned_at`, `idx_checkin_logs_result`

### Migration Status

- Applied: **YES** (manually by user)
- Verified: Confirmed by user
- Rolled back: No

### Tests Performed

- Schema created and applied to Aiven PostgreSQL by user manually.
- Database tables (events, scanner_devices, event_passes, checkin_logs) and their indexes were verified successfully using the `backend/database/verify_schema.js` script.

### Important Decisions

- **No Prisma** — followed existing raw SQL pattern.
- `qr_token` is UNIQUE at DB level AND the check-in API will use `SELECT FOR UPDATE` inside a transaction — two independent layers of concurrent duplicate protection.
- `checked_in_by` and `gate` are stored directly on `event_passes` (denormalised at check-in time) so the audit record survives scanner device deletion.
- `checkin_logs.pass_id` is nullable — allows logging `INVALID` scan attempts where the token is not in the DB.
- `scanner_devices.event_id` is nullable — a scanner can be event-agnostic if needed.
- `event_passes` uses `ON DELETE RESTRICT` for `event_id` — prevents accidental event deletion while passes exist.

### Known Issues

- None.

---

## Phase 2 Checkpoint

Phase: PHASE 2 — Secure QR Token Generation

Status: **COMPLETE**

### Completed

- Implemented token generation utility using cryptographically secure random bytes (256 bits of entropy, 64-character hex string).
- Designed the human-readable pass number generator using the structure `<PREFIX>-<YEAR>-<SEQUENCE>` (padded to 4 digits).
- Added a `getNextPassSequence` database utility to query/derive the next sequence number inside a transaction.
- Designed a scanner password generator using a cryptographically secure random integer charset excluding ambiguous characters (0, O, 1, I, l).
- Created a `verifyPassRecord` helper logic validating status flow constraints (issued, checked in, cancelled, and event mismatches) and mapping them directly to `checkin_logs` results.
- Built a unit test suite to test all components of token generation and pass verification logic.

### Files Modified/Created

- `backend/src/utils/passQRUtils.js` ← **new utility file**
- `backend/database/test_pass_qr_utils.js` ← **new test file**

### Token Design Decisions

- **Stored Raw in DB:** Instead of hashing the token, the raw 64-char hex string is stored directly in `event_passes.qr_token`. This allows O(1) indexed lookup when the scanner sends the token. Security is maintained by using a cryptographically secure random generator (256 bits of entropy) and transport-layer encryption (HTTPS POST body only).
- **Decoupled Validation Logic:** The verification logic is fully encapsulated in `verifyPassRecord()`, which makes it testable without DB access and ensures the exact same verification rule is used by both the check-in API and the manual check-in process.

### Tests Performed

- Executed `node database/test_pass_qr_utils.js` checking:
  - Token uniqueness and 64-character hex format (PASSED).
  - Pass number format with sequence padding (PASSED).
  - Unambiguous password generation (PASSED).
  - Null pass validation logic mapping to `INVALID` (PASSED).
  - Cancelled status mapping to `CANCELLED` (PASSED).
  - Checked-in status mapping to `ALREADY_CHECKED_IN` (PASSED).
  - Wrong event mapping to `WRONG_EVENT` (PASSED).
  - Valid pass mapping to `SUCCESS` (PASSED).

### Next Step

**PHASE 3 — Backend API Development**

---

## Phase 3 Checkpoint

Phase: PHASE 3 — Backend API Development

Status: **COMPLETE**

### Completed

- Implemented full CRUD controllers and routes for Events (`events` table).
- Implemented single Pass creation, query listing (with search and filters), cancellation, and reissue endpoints.
- Implemented bulk Guest/Pass import endpoint.
- Integrated all routes into the main Express application routing scheme in `server.js`.
- Implemented database cleanups in the test suite to keep the DB free of test relics.

### Files Modified/Created

- `backend/src/routes/eventPassRoutes.js` ← **new route file**
- `backend/src/controllers/eventPassController.js` ← **new controller file**
- `backend/src/server.js` ← **mounted router**
- `backend/.env.example` ← **new configuration template**

---

## Phase 4 Checkpoint

Phase: PHASE 4 — Check-In API

Status: **COMPLETE**

### Completed

- Created check-in processing endpoint `POST /api/scanner/checkin` and manual fallback check-in `POST /api/event-passes/manual-checkin`.
- Designed robust concurrent check-in security using PostgreSQL row-level locks (`SELECT ... FOR UPDATE`) wrapped inside a transaction.
- Standardized response schemas matching all state conditions (SUCCESS, ALREADY_CHECKED_IN, CANCELLED, INVALID, WRONG_EVENT).
- Implemented append-only audit trail logger writing every transaction outcome into the `checkin_logs` table.

### Files Modified/Created

- `backend/src/controllers/checkInController.js` ← **new check-in controller**

---

## Phase 5 Checkpoint

Phase: PHASE 5 — Scanner Authentication

Status: **COMPLETE**

### Completed

- Designed a secure device-login credential system.
- Created `verifyScanner` middleware enforcing `QR_PASS_JWT_SECRET` JWT check, role constraint checks, and database-level `is_active` validation of scanner devices.
- Created profile detail endpoint `GET /api/scanner/me`.
- Added automatic activity tracking logging timestamps (`last_seen_at`) on scanner logins and validated access attempts.

### Files Modified/Created

- `backend/src/middleware/scannerAuth.js` ← **new auth middleware**
- `backend/src/controllers/scannerAuthController.js` ← **new scanner auth controller**
- `backend/src/routes/scannerRoutes.js` ← **new scanner route file**

### Tests Performed

A comprehensive E2E integration test suite (`backend/database/test_event_pass_apis.js`) was written and run successfully, verifying:
- Event creation, listing, and updates.
- Scanner device registration, bcrypt password hashing, and token-based login.
- Accessing profile details via authenticated scanner tokens.
- Secure pass generation.
- Valid scan check-ins returning `SUCCESS`.
- Duplicate check-ins blocked instantly returning `ALREADY_CHECKED_IN` (atomic check).
- Check-in attempts on cancelled passes returning `CANCELLED`.
- Pass reissues generating a new token and resetting status back to `ISSUED`.
- Bulk guest pass importing.
- Manual admin check-ins returning `SUCCESS`.
- Retrieving live attendance stats and scan logs.
- Scanner deactivation disabling the device token immediately (returning `403 Forbidden`).
- Automatic test database cleanup leaving zero junk records.

---

## Phase 6 Checkpoint — FINAL STATUS

Phase: **PHASE 6 — Admin Panel (Frontend UI Integration)**

Status: **COMPLETE — VERIFIED**

### Completed

The Event Pass Admin Panel has been successfully implemented and the original
1500+ line monolithic `EventPassAdminTab.jsx` has been refactored into a
modular feature structure under:

```text
ngo-web/src/components/event-passes/
```

The Event Pass module now follows the same general feature-component approach
used by the existing Evaluation module.

### Files Created

```text
ngo-web/src/components/event-passes/
├── EventPassAdminTab.jsx
├── EventsManager.jsx
├── PassDetail.jsx
├── PassList.jsx
├── BulkImport.jsx
├── AttendanceDashboard.jsx
├── ScannerDevices.jsx
└── eventPassHelpers.js
```

### Files Modified

```text
ngo-web/src/pages/AdminDashboard.jsx
```

`AdminDashboard.jsx` now registers the Event Pass module and uses the new
modular `EventPassAdminTab` implementation.

### Files Deleted

```text
ngo-web/src/components/admin/EventPassAdminTab.jsx
```

The old monolithic Event Pass component was removed after the modular
implementation was completed.

### Architecture Decision

`EventPassAdminTab.jsx` is now the Event Pass feature's orchestration layer.
Feature-specific functionality is separated into focused components rather
than being maintained in one very large component.

```text
AdminDashboard
      ↓
EventPassAdminTab
      ↓
├── EventsManager
├── PassList
├── PassDetail
├── BulkImport
├── AttendanceDashboard
├── ScannerDevices
└── eventPassHelpers
```

Do not merge these components back into one large `EventPassAdminTab.jsx`.

### Existing Backend/API Work Preserved

Phases 1–5 remain complete and must not be rebuilt.

The existing Event Pass API namespace in:

```text
ngo-web/src/utils/api.js
```

must continue to be reused.

Do not create a duplicate API client or duplicate Event Pass API methods.

### Tests Performed

The frontend production build was run successfully:

```text
npm run build
```

Result:

```text
SUCCESS
Zero compilation/lint errors
```

### Phase Status Summary

```text
Phase 1 — Database Migration           COMPLETE
Phase 2 — Secure QR Token Generation   COMPLETE
Phase 3 — Backend APIs                  COMPLETE
Phase 4 — Check-In API                  COMPLETE
Phase 5 — Scanner Authentication        COMPLETE
Phase 6 — Admin Panel                   COMPLETE
Phase 7 — QR / Printable Pass           COMPLETE
Phase 8 — Attendance Dashboard          COMPLETE
Phase 9 — Manual Check-In               COMPLETE
Phase 10 — Backend Testing              COMPLETE
Phase 11 — Expo Scanner APK             COMPLETE
```

### Known Issues

- No known Phase 6–11 frontend, backend, or app build issues.
- Do not rerun the already-applied database migration.

### Next Step

**Final End-to-End / Production Verification**

This is the final phase before deployment. It involves verifying that all components (backend, web admin dashboard, and scanner APK) work together seamlessly in a production-like environment.

### Phase 11 Completion Status

- **API Client:** Implemented in `src/api/` with `client.ts` configured for the `EXPO_PUBLIC_API_URL` and `AuthContext` to manage `SecureStore` persistence of JWT.
- **Login Screen:** Added `app/login.tsx` for secure device code and password login.
- **Scanner Dashboard:** Added `app/index.tsx` for displaying active event details and scanner assignments, providing a quick launch into scanning mode.
- **Camera Scanner:** Added `app/scanner.tsx` leveraging `expo-camera` with proper permissions checking, QR parsing, duplicate scan prevention (UI lock), and API requests.
- **Result UI:** Added `app/result.tsx` with dynamic styling mapping to states (`SUCCESS`, `ALREADY_CHECKED_IN`, `INVALID`, `CANCELLED`, `CONNECTION_ERROR`).

### Subsequent Phase Order

```text
Phase 11 Expo Scanner APK               (COMPLETE)
   ↓
Final End-to-End / Production Verification ← NEXT
```

---

## Final Handoff Status

The QR Event Pass backend, Admin Panel, Printable Pass formats, Attendance Dashboard, Manual Check-In methods, Backend Testing, and Expo Scanner APK are complete through **Phase 11**.

The project is now ready to move to:

**Final End-to-End / Production Verification**

### Recovery Rule

Before making final verification changes, inspect:

```bash
git status
git diff
git log --oneline -10
```

Then read:

```text
CLAUDE.md
CONTEXT.md
```

Do not discard working changes and do not rerun completed migrations.

### Overall Workflow

```text
Phase 1  Database                           (COMPLETE)
   ↓
Phase 2  Secure QR Tokens                   (COMPLETE)
   ↓
Phase 3  Backend APIs                       (COMPLETE)
   ↓
Phase 4  Check-In API                       (COMPLETE)
   ↓
Phase 5  Scanner Authentication             (COMPLETE)
   ↓
Phase 6  Event Pass Admin Panel             (COMPLETE)
   ↓
Phase 7  QR / Printable Pass                (COMPLETE)
   ↓
Phase 8  Attendance Dashboard               (COMPLETE)
   ↓
Phase 9  Manual Check-In                    (COMPLETE)
   ↓
Phase 10 Backend Testing                    (COMPLETE)
   ↓
Phase 11 Expo Scanner APK                   (COMPLETE)
   ↓
Final End-to-End / Production Verification  ← NEXT
```


Phase 11 is the final implementation phase. After the scanner APK is built and
verified end-to-end, final production verification should be performed.


